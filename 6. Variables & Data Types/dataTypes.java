public class dataTypes {
    public static void main(String[] args) {
        byte b= 8; //only 256 no is allowed in byte.
        System.out.println(b);
        char ch='a';// only one character is allowed in char type data.
        System.out.println(ch);
        boolean var = true;
        boolean vari = false;
    }
}
